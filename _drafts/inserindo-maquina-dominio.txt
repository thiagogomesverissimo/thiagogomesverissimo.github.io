#Colocando máquina no domínio
1)Corrigir horário do computador:
  "alterar configurações de data e hora" -> "Horário de internet" -> "Servidor: fflch.local" -> "Atualizar agora".

2)Rodar script de configuração hora.

2*) Usando o Profwiz: converte usuário e coloca a máquina no domínio.

2**)Propriedade do Meu computador -> Altera configurações -> Alterar:
 2.1)Corrigir nome do computador para padrão SIGLA_SETOR-N.PATRIMONIO.
 2.2)Domínio: fflch.local
 2.3)Inserir número USP e senha. 
 2.4)Reinicia máquina.

Clonar perfil do padrão (falta criar) para perfil 'default' (o perfil 'default está oculto').
resolve problema da cannon, login mais rápido.

RSAT: http://www.microsoft.com/en-us/download/details.aspx?id=7887

1)Criar Grupo e pasta compartilhada no zentyal (com mesmo nome).
Criar pasta compartilhada.
Exemplo sti

#Não funciona por nome, só por IP ou arquivo VBS? BAT
net use H: \\172.16.0.28\%username%
net use M: \\172.16.0.28\scinfor-08

GPO-RSAT:
Gerenciamento de Diretiva de grupo.
Clicar em objetos de diretiva de grupo. novo -> nome do grupo definido no zentyal.
em filtros de segurança remover a opção: "usuário autenticados."
adicionar->avançado->localixar agora e procurar grupo.ok
subir o arquivo de logon->editar GPO->Configurações do Usuário->Diretivas->Configurações do Windows->Scripts->logon->adicionar arquivo.
depois disso tudo, arrastar GPO para domínio fflch.local.
ele jogou depois do "Default Domain Policy"(pois essa é a politica defauk do windows, depois dela nada é executado), mover para cima.

faz upload da GPO do grupo pelo zentyal (script logon).
no RSAT, aplicar a GPO no grupo: 

3)Diretórios:
 3.1)Conferir se a pasta do usuário foi mapeada para H e sem atalho no desktop.

#Tarefas no servidor
1)Conferir horário do servidor # DATE. Corrigir horário do servidor, como?

sudo pdbedit -x -m THIAGOSTIVBOX
sudo userdel NAME_OF_COMPUTER_TO_REMOVE\$ 

1) Arrumar horário do servidor (ver como foi feito?).
2) VOLUMES GRANDES (lvm, fstab no pasta do usuario e do grupo, DELL), aumentar partição home do zentyal.
3) Atualização do zentyal.
3.1) Instalação de uma versão 3.5 (atual), subi um backup nela (importar e exportar configurações).
3.2) Instalação de uma versão 3.5 (atual), e upgrade para 4.
3.2) Instalação de uma versão 3.5 (atual), sobe backup e upgrade para 4

FAZER:
(deidson vai arrumar script)
*Horário automático nos services do windows
**Criar atalho da pasta pessoal do servidor no desktop

problema: se usuário marcou como rede pública, marcar como rede de domínio.
Falta ver como apaga diretório compartilhados criados e não usados mais.


RSAT: http://www.microsoft.com/en-us/download/details.aspx?id=7887

1)Criar Grupo e pasta compartilhada no zentyal (com mesmo nome).
Criar pasta compartilhada.
Exemplo sti

#Não funciona por nome, só por IP ou arquivo VBS?
net use H: \\172.16.0.28\%username%
net use M: \\172.16.0.28\scinfor-08

GPO-RSAT:
Gerenciamento de Diretiva de grupo.
Clicar em objetos de diretiva de grupo. novo -> nome do grupo definido no zentyal.
em filtros de segurança remover a opção: "usuário autenticados."
adicionar->avançado->localixar agora e procurar grupo.ok
subir o arquivo de logon->editar GPO->Configurações do Usuário->Diretivas->Configurações do Windows->Scripts->logon->adicionar arquivo.
depois disso tudo, arrastar GPO para domínio fflch.local.
ele jogou depois do "Default Domain Policy"(pois essa é a politica defauk do windows, depois dela nada é executado), mover para cima.

faz upload da GPO do grupo pelo zentyal (script logon).
no RSAT, aplicar a GPO no grupo: 

3)Diretórios:
 3.1)Conferir se a pasta do usuário foi mapeada para H e sem atalho no desktop.

#Tarefas no servidor
1)Conferir horário do servidor # DATE. Corrigir horário do servidor, como?

sudo pdbedit -x -m THIAGOSTIVBOX
sudo userdel NAME_OF_COMPUTER_TO_REMOVE\$ 

